[package]
name = "aurora"
version = "2.0.0"
edition = "2021"

[dependencies]
slint = { version = "1.16.1", features = ["backend-winit"] }
backend = { path = "../backend" }
shared = { path = "../shared" }
display-info = "0.5"
log = { workspace = true }

[build-dependencies]
slint-build = { version = "1.16.1" }
image = "0.25"
