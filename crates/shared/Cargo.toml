[package]
name = "shared"
version = "2.0.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
scandir = "2.0"

log = { workspace = true }
env_logger = "0.11"
env_filter = "1.0"
chrono = "0.4"
termcolor = "1.4"

anyhow = { workspace = true }
